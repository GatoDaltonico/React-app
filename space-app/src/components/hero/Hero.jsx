import './Hero.css';
import Typography from '@mui/material/Typography';
import React from 'react';

function Hero() {
    return (
        <div className="hero">
            <Typography variant="h1">
                Bienvenidos a la NASA (que no es NASA)
            </Typography>
        </div>
    );
}

export default Hero;
